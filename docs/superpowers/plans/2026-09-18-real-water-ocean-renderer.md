# Real-Water Ocean Renderer Plan

This historical execution plan has been superseded for the remaining work.

Use the root-level `ASTRA_HANDOFF.md` as the authoritative continuation brief. It contains the current implementation state, real-water renderer cutover, clean-room ocean optimization direction, Small Ships-only vessel integration scope, minimal quick-check policy, and the exact point where automated work must stop for human Minecraft testing.

Shippy Ships is explicitly out of scope and must not be added.

Do not re-run the old large RED/GREEN matrix or manual QA plan from earlier revisions unless the human explicitly asks for it.
